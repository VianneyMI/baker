"""`baker.schemas.intensity` module."""

from enum import StrEnum

class Intensity(StrEnum):
    """`baker.schemas.intensity.Intensity` enum."""
    
    low = "low"
    medium = "medium"
    high = "high"
